# Drift Model

The drift system must implement a baseline particle advection-diffusion engine with configurable wind/current coefficients, timestep, particle count, and uncertainty perturbations.

Outputs should be regions, corridors, release windows, and confidence values rather than false-precision coordinates.
